import { createSlice } from "@reduxjs/toolkit";

export const affiliationSlice = createSlice({
  name: "affiliation",
  initialState: {},
  reducers: {},
});

export const {} = affiliationSlice.actions;
export const affiliationReducer = affiliationSlice.reducer;
