import { createSlice } from "@reduxjs/toolkit";

export const exploreMapSlice = createSlice({
  name: "explore-map",
  initialState: {},
  reducers: {},
});

export const {} = exploreMapSlice.actions;
export const exploreMapReducer = exploreMapSlice.reducer;
