import { createSlice } from "@reduxjs/toolkit";

export const scoutaiSlice = createSlice({
  name: "scoutai",
  initialState: {},
  reducers: {},
});

export const {} = scoutaiSlice.actions;
export const scoutaiReducer = scoutaiSlice.reducer;
