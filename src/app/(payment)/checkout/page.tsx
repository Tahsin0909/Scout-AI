import CheckoutClient from "@/features/payment/components/CheckoutClient";

const page = () => {
    return (
        <div>
            <CheckoutClient />
        </div>
    );
};

export default page;