import PartnerContent from '@/features/partnership/components/dashboard/PartnerContent';

const page = () => {
    return (
        <div>
            <PartnerContent />
        </div>
    );
};

export default page;